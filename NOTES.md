# Claude Pet 实现笔记

一份"这个桌宠是怎么做出来的"的说明，偏**设计取舍和踩过的坑**，不是 API 手册。
配套文档：`README.md` 是使用说明 + 已知坑清单，本文是**为什么这么做**。

代码规模（供参考）：

| 文件 | 行数（约） | 干什么 |
|---|---|---|
| `src\Pet.cs` | 1696 | 桌宠本体：窗口、状态机、动画、UDP、交互、配置 |
| `src\Cutout.cs` | 641 | 抠图工具（素材流水线第 1 步） |
| `src\MakeSprites.cs` | 461 | 占位素材生成器（退路） |
| `src\SheetGen.cs` | 403 | 生成 sprite sheet + 清单（第 2 步） |
| `src\PetNotify.cs` | 188 | hook 转发器（热路径，极简） |
| `src\Zoom.cs` | 140 | 调试用：裁块放大 |
| `src\Common.cs` | 51 | 共用源码（PNG 保存 / 建目录），**被编进**上面 5 个 exe |

（这行数以前写的是 1402，早就不对了 —— 所以标上"约"，别当精确值用。）

---

## 1. 整体架构

```
Claude Code 会话
    │  每次事件（会话开始、提交输入、跑工具、等你确认、答完…）
    ▼  spawn 一个进程（exec 形式，不经过 shell）
PetNotify.exe            ← 186 行，无 WPF 依赖，启动快
    │  读 stdin 的 hook JSON → 映射成状态名 → 发一个 UDP 包
    ▼  127.0.0.1:47821   载荷 = 状态|工具名|会话id|备注
Pet.exe                  ← 常驻；监听 UDP，切状态、播动画、冒气泡
    │
    ▼
assets\pet.json + assets\pet.png     ← 清单 + 精灵图（桌面宠物的"数据"全在这两个文件里）
```

素材流水线（换形象时走这条，和上面的运行时完全解耦）：

```
一张人物图（JPEG/PNG 都行）
    │  Cutout.exe   抠图：去掉背景、裁到人物 bbox、保持原始分辨率
    ▼
assets\cut\<姿势>.png + 三张诊断图 + 一份数值诊断
    │  SheetGen.exe 每个状态一张图 → 一行动画 → 拼成 sheet，并写清单
    ▼
assets\pet.png（8 列 × 8 行，单帧 288×384px） + assets\pet.json
```

**为什么拆成这么多小工具**：每一步都用肉眼能验的中间产物（诊断图、离屏渲染）隔开。
抠图是最容易出错的一步，拆开之后它可以单独返工，不用每次重跑动画。

---

## 2. 桌宠本体（Pet.cs）

### 2.1 窗口：透明、置顶、**不抢焦点**

```csharp
win.WindowStyle = WindowStyle.None;
win.AllowsTransparency = true;
win.Background = Brushes.Transparent;
win.Topmost = true;
win.ShowInTaskbar = false;
win.ShowActivated = false;     // ← 关键
```

> **知识点：让窗口"点它不抢焦点"要两步，`ShowActivated=false` 一个人做不到。**
> 普通窗口被点击时会抢走焦点 —— 对一个"趴在桌面角落"的宠物来说这是灾难：
> 你正打字，点一下它，输入框就失焦了。
>
> **踩过的坑**：这个项目一直声称自己是 `WS_EX_NOACTIVATE` 窗口，README 也这么写 ——
> 但**那是假的**。`ShowActivated=false` 只管第一次 `Show()` 时不激活，
> WPF **不会**因此给窗口加 `WS_EX_NOACTIVATE` 样式。实测 `GetWindowLong(GWL_EXSTYLE)`
> 出来是 `0x00080008`（TOPMOST|LAYERED），压根没有 `0x08000000`。
> 于是点击时 Windows 照样发 `WM_MOUSEACTIVATE` 要求激活，焦点就被抢走了 ——
> 整个项目最核心的体验承诺，其实一天都没生效过。**文档里的断言也要验证，尤其是"最重要"的那条。**
>
> 正确做法：**`WS_EX_NOACTIVATE` 样式 + `WM_MOUSEACTIVATE → MA_NOACTIVATE` 钩子，两个都要**
> （见 `DontStealFocusOnClick`）。提示卡片窗口也走同一条路。
>
> **这一条踩得最久（前后六版），值得完整看一遍失败过程**：
>
> | 做法 | 焦点 | 菜单 | 为什么不行 |
> |---|---|---|---|
> | `ShowActivated=false` | ❌ | ✅ | 只管第一次 Show 不激活，点击照样抢焦点 |
> | `WM_MOUSEACTIVATE → MA_NOACTIVATE` 钩子 | ❌ | ✅ | 只拦"点击引发的激活"；**WPF 处理鼠标输入时自己还会主动激活窗口**，走另一条路 |
> | `WS_EX_NOACTIVATE` 样式 | ✅ | ❌ | 样式生效了，但它让窗口**永远不可能失活** —— WPF 菜单关闭的默认逻辑靠的正是"宿主失活" |
> | 样式 + `Mouse.Capture(menu, SubTree)` | ✅ | ❌ | 这个组合下捕获不起作用 |
> | 样式 + 采样鼠标 × 菜单矩形 | ✅ | ❌ | `PointToScreen`+`ActualWidth` 算出的菜单矩形不可靠（拿到 1×1），菜单内部的点击也被判成"外面" |
> | 样式 + 采样鼠标 + 菜单项"认领"点击 | ✅ | ❌ | 又踩两个：打开菜单那一次按键的粘性位被当成"点击"（菜单刚弹出就自己关掉）；按住菜单项超过宽限时间也被判成"外面" |
> | 样式，但菜单打开期间临时撤掉 | ✅ | ❌ | 菜单项好用了、焦点也还回去了，但**点空白处还是关不掉** |
> | **样式 + 菜单期间临时撤掉 + 全屏"点击接收层"** | ✅ | ✅ | **最终解法**：让操作系统的命中测试去决定里外（见下） |
>
> **三条教训**：
> ①**别用"代理指标"代替结果验证**：我曾用 `SendMessage(WM_MOUSEACTIVATE)` 返回 3 当作
> "焦点保住了"的证据 —— 指标没错，焦点照样被抢。要验"焦点有没有丢"就去真打字。
> ②修完一个"守承诺"的改动，**换个角度再验一遍**（"我原来的功能还活着吗"）。
> ③**一个机制说不通时，回头找根因，别一路修补后果** —— 这条最贵。
> 中间那五版每一版都在解决上一版的边界情况（粘性位、矩形、按住时长……），
> 而真正的麻烦只有一个：**那个样式和 WPF 菜单的前提冲突**。
> 想明白之后，"菜单期间临时放开"一句话就解决了，顺手删掉了前面约 140 行补丁。
> **判断标准：给同一个东西加第 3 个补丁时，停下来问"是不是前提错了"。**
> ④**查社区比继续猜快得多**：最后那一步（点空白处关不掉）卡住时去搜了一下，
> 一下子就拿到了根因 —— **WPF 的 `ContextMenu` 把关闭挂在"鼠标捕获"上**：
> "它捕获鼠标，而控件被写死成'鼠标一失去捕获就关'，算设计缺陷；可以用原生 `Popup` 绕开。"
> 这解释了两件我没想通的事：为什么"临时放开激活"能让菜单项好用、却依然关不掉
> （关闭压根不走激活那条路），以及为什么我试着 `Mouse.Capture` 反而更糟。
> 解法也就清楚了：**别再用它的机制，改成让操作系统的命中测试决定里外** ——
> 铺一层几乎全屏的接收层（`alpha=1` 的透明窗口，全透明会穿透接不到点击），
> 用 `SetWindowPos` 排在菜单后面，任何落在菜单外的点击就打到它身上 → 关菜单。
> **知识点：分层窗口（Layered Window）截不到图**
> `AllowsTransparency=true` 的 WPF 窗口走的是 `UpdateLayeredWindow`，
> `CopyFromScreen`/`PrintWindow` 抓到的是空白或残影。
> 所以**验证渲染只能靠"离屏渲染"**：`Pet.exe --shot <状态> <输出.png>` 把同一套界面
> 画到 `RenderTargetBitmap` 上导出成 PNG，同时把各元素尺寸写进同名 `.txt`。
> 这条限制后来逼出了一整套验证方法（见第 6 节）。

### 2.2 状态机

状态名来自 hook，**不可信**，所以进来先过白名单：

```csharp
if (!anims.ContainsKey(s)) s = "idle";   // 清单里没有的状态 → 安全回退，绝不崩
```

三类状态的生命周期**不一样**，这是设计上最容易搞混的地方：

| 类别 | 状态 | 行为 |
|---|---|---|
| **持久** | `idle` `think` `work` `alert` `sleep` | 一直保持，直到下一个事件把它换掉。`alert`（等你确认）会举着手不放，`sleep` 会一直睡 |
| **一次性** | `done` `awake` `error` | 播完停在最后一帧，`DecayMs` 到点后自动回 `idle`（3500 / 2500 / 5000 ms） |
| **兜底** | 任意 | 「卡在 think/work 超过 90 秒」→ 判定会话异常结束，回 `idle`（`StaleMs`） |

> **知识点：一次性动画为什么要用 `t = f/(n-1)` 而不是 `f/n`**
> 动画帧的相位 `t` 决定姿势。非循环动画播完会**停在最后一帧**并保持几秒，
> 如果用 `f/n`，最后一帧的 `t` 是 `0.875` 而不是 `1`，角色会**停在半空中**。
> 用 `f/(n-1)` 则最后一帧正好落在静止位。

非循环动画的"回去"目标放在 `pendingState` 里，由 500ms 的 tick 定时器检查
`animTimer.IsEnabled`（动画停了才轮到衰减判断），这样"播完"和"停够时间"是两个独立的阶段。

### 2.3 精灵图与动画（清单驱动）

`pet.json` 把"怎么切图"和"怎么播"全部数据化：

```json
{
  "frameWidth": 288, "frameHeight": 384, "sheetScale": 2,
  "animations": {
    "done": { "row": 4, "frames": 8, "fps": 12, "loop": false }
  }
}
```

> **知识点：把"素材像素尺寸"和"屏幕显示尺寸"分开**
> `frameWidth/frameHeight` 是 sheet 里的**像素**尺寸（切图用），
> `sheetScale` 是像素密度，**屏幕上的逻辑尺寸 = frameWidth / sheetScale**。
> 这样素材可以按 2 倍密度生成（100% DPI 下是超采样、把桌宠放大到 2 倍也不糊），
> 而程序里的布局数学始终用逻辑像素。

窗口高度 = 人物高 + `LabelHeight`(28)，那 28px 是**气泡区**（气泡是程序画的，不在素材里）。
气泡宽度上限跟着窗口宽度走，所以换更宽的形象时不用改代码。

### 2.4 交互

| 操作 | 实现 |
|---|---|
| 左键单击 | `SayLine()` —— 她冒一句话（台词随状态轮换），**状态不变**，气泡带"冒出来"动画 |
| 左键拖动 | `win.DragMove()`（见下面那条坑） |
| 右键单击 | `CycleState()` —— 切到下一个状态（**延迟 350ms**，见下） |
| 右键双击 | `OpenPetMenu()` —— 菜单：大小 / 透明度 / 回到右下角 / 重新载入素材 / 测试各状态 / 开机自启 / 全屏自动隐藏 / **隐藏到托盘** / 退出 |
| 托盘图标双击 | 从「隐藏到托盘」状态恢复 |
| 再双击 exe | 单实例"召唤"：让已在跑的那只冒泡 + 从屏幕外挪回来 |

> **知识点：把"有后果"的操作和高频操作分开**
> 2026-09-12 之前是"左键切状态、右键弹菜单"。改版后左键只说话（不改变任何状态），
> 切状态挪到右键、菜单挪到右键双击。**左键是最高频的按键，留给"可以随便点"的动作** ——
> 这样连点宠物不会把你的状态弄乱，只是想听她说话。

> **知识点：单双击的区分是"信息不够"，不是"实现不好"**
> 想同时支持"右键单击"和"右键双击"，系统就必须**等你不再点第二下**才能确定这是单击。
> 所以右键切状态有 `RightDblClickMs`(350ms) 的迟滞 —— 这段延迟是问题本身的性质，
> 换成"先切状态、发现是双击再撤销"只会让状态肉眼闪一下，更糟。
> **判据**：不能消除的延迟，就在设计上认下来并写进文档，别假装它不存在。

> **知识点：动画和"离屏渲染验证"是天然相冲的**
> `ShotMode()` 渲染的是**当前这一帧**。气泡要是在跑"从 0.72 倍放大 + 淡入"的动画，
> 截出来就是缩着的半透明气泡，离屏验证直接失去意义。
> 所以 `ShowLabel(text, animate)` 的 `animate` **默认 false**：`--shot` 和 hook 气泡走这条
> （显式把缩放复位、不透明度置 1），只有左键说话才传 true。
> **通用做法：凡是"让界面动起来"的改动，都要给验证路径留一个静止档。**

> **知识点：托盘图标（NotifyIcon）的四个坑**（社区里反复出现的）
> ① `csc.rsp` 默认已经引用了 `System.Windows.Forms` / `System.Drawing`，
> **别再显式 `/r:`**（CS1703 重复程序集）；② `Icon` 不设或 `Visible` 不为 true，图标**不显示也不报错**；
> ③ 退出前必须 `Dispose()`，否则托盘留一个**幽灵图标**；④ `Text` 上限 63 字符。
> 另外写这两个命名空间的类型时**全用全名别加 `using`** —— 它们和 `System.Windows.Media`
> 撞了 `Color`/`Brush`/`Point`/`Size`/`Application` 一堆名字。详见 README 坑 25。

> **知识点：两套"隐藏"必须是两个字段**
> 「全屏游戏时自动隐藏」和用户主动的「隐藏到托盘」看起来都是 `win.Hide()`，但语义不同：
> 前者会自己恢复，后者**不能**自己恢复。共用一个字段的话，你手动藏起来之后切出全屏，
> 全屏那套恢复逻辑会把宠物弹回来。两者**唯一的交叉点**是 `CheckFullscreen()` 开头那句
> `if (hiddenByUser) return;`。

> **知识点：右键菜单的"点外面就关"是自己接住点击实现的**，不是靠 `ContextMenu` 自己的逻辑。
> 细节见 2.1 那条坑：`ContextMenu` 把关闭挂在**鼠标捕获**上，在我们这种"窗口不激活"的场景里
> 不可靠（社区原话：设计缺陷）。做法是菜单打开期间铺一层**几乎全屏的点击接收层**，
> 让**操作系统的命中测试**决定里外 —— 不再猜。同时菜单打开期间会临时撤掉
> `WS_EX_NOACTIVATE`（否则菜单项点不动），关闭时再把样式装回来并把**焦点还给原来那个窗口**。
> 三件事缺一不可，而且都验证过；改这里之前把 2.1 那整段读完。

> **坑：判断"单击还是拖拽"，要比窗口坐标，不能比鼠标相对窗口的位置。**
> 最初写成在 `DragMove()` 前后比较 `e.GetPosition(win)` —— 但 `DragMove()` 内部会移动窗口，
> 这个差值会被污染，**原地单击也会被误判成拖拽**（症状：点一下宠物，它被挪走而不是切状态，
> 还把错位置写进了 config.json）。改成比较 `win.Left/Top`（阈值 4px）就对了。

> **知识点：单实例 + "召唤"协议为什么必须等 ack**
> 用命名 Mutex 做单实例。第二只启动时不能只是"发个消息就退出" ——
> 万一第一只虽然占着 Mutex 却没在监听（卡住、端口被占），用户看到的就是
> **双击毫无反应**，而这是他最可能来问的故障。现在的做法：发 hail，等 1.5 秒 ack；
> 等不到就弹一张自绘的提示卡片告诉他怎么办。**"静默失败"是桌宠这类小工具最大的体验杀手。**

> **知识点：提示不要用 `MessageBox`**
> 不是为了防它失效，而是它**抢焦点**：模态框会挡住你、还要你专门去点掉它。
> 桌宠整个设计就是不打断你，所以提示用自绘的无按钮卡片（`Topmost` + `ShowActivated=false`
> + 8 秒后自己 `Close()`）——没有「确定」可点，也就不用为一条提示挪鼠标。

### 2.5 位置与显示

- 位置、大小、透明度存在 `config.json`（全部留在 D 盘）。
- **启动时和"被召唤"时都会检查"是不是看得全"**：存下来的坐标可能因为改分辨率、
  拔显示器而落到屏幕外，那时宠物会**一声不吭地活在屏幕外**（进程在、UDP 在听、
  桌面上什么都没有）—— 看起来就像"程序根本没启动"。
- 时机上有个坑：这个检查必须在 `win.Show()` **之后**做。放在 `BuildWindow()` 里会按
  一个还没定下来的窗口尺寸判断，反而把宠物挪到奇怪的地方（实测过）。

> **知识点：窗口坐标是逻辑像素，屏幕坐标是物理像素**
> 100% DPI 下两者相等，所以容易写出"碰巧能跑"的代码。真要换算用
> `PresentationSource.FromVisual(win).CompositionTarget.TransformToDevice.M11`。

### 2.6 全屏游戏时自动躲开

进全屏游戏自动隐藏、退出全屏自动回来，位置不变。开关在右键菜单里，默认开。

**先想清楚"要解决的到底是什么"**：真·独占全屏（游戏接管显示输出）下 DWM 根本不会合成
别的窗口，桌宠在那个模式下**压根画不出来** —— 检测失败也无害，失败模式是安全的。
所以这个功能真正要解决的只有**无边框窗口化全屏**（Windows 仍当普通窗口，桌宠会盖上去），
而那恰恰是窗口矩形能精确测出来的模式。**这一条决定了整个设计的容错余量**
（可以放心用"判不出来就当不是全屏"这种保守策略）。

判定：500ms 轮询 `GetForegroundWindow`，看它的矩形和它所在显示器的 `rcMonitor`
是否**精确相等**，再加一串排除项。为什么是精确相等、为什么排除项那么多，
README 坑 20 有全部实测数据。**用轮询不用 `SetWinEventHook`**：游戏常以管理员权限跑，
非提权进程注册的前台事件钩子收不到通知。

**防抖**：连续 2 次同向采样才动作（约 1 秒），避免 alt-tab 途中闪一下。

**救生圈（这一块别省）**：藏起来之后用户**右键不到它**，菜单里那个开关等于够不着。
所以 `SummonSelf()`（双击 `Pet.exe`）在"被本功能藏起来了"的情况下，
除了叫回来还会**把这个功能关掉**并气泡说明 —— 不关掉的话下一轮采样又把它藏回去，
用起来就像"双击没用"。**"能关掉"和"能叫回来"必须是两条独立的路。**

`ShowPetAfterFullscreen()` 里重新 `win.Show()` 之后**必须补一次 `DontStealFocusOnClick`** ——
`Show()` 会重新应用窗口样式、把 `WS_EX_NOACTIVATE` 冲掉，不补就会"从游戏回来之后点它抢焦点"。
这个坑在菜单接收层上已经踩过一次（见 2.1），是同源问题。

### 2.7 开机自启

两个入口，**读写同一个注册表值**，所以永远不会不同步：

- 桌宠**右键菜单 →「开机自启」**（带勾选；勾选状态每次打开菜单时现读注册表）
- `install-autostart.ps1`（`-Status` / `-Disable`）

```
HKCU\Software\Microsoft\Windows\CurrentVersion\Run\ClaudePet = "D:\ClaudePet\bin\Pet.exe"
```

> **为什么用注册表键而不是"启动"文件夹**：启动文件夹在 C 盘
> （`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup`），
> 而这个项目的原则是 **C 盘零占用**。注册表项不新增任何文件，
> 而且任务管理器的「启动」标签页里一样能看到、能禁用（两处是同一个开关）。

---

## 3. 素材流水线

### 3.1 抠图（Cutout.cs）—— 这个项目里技术含量最高的一块

**输入**：一张 1792×2336、**没有 alpha 通道**、人物画在一块**纯青色**背景上的 Q 版角色图。

**算法只有一句话**：把"就是背景色"的像素全删掉。

```
1) 背景参考色 = 图像最外 20px 那一圈的均色（那里铁定是背景）
2) 逐个像素：和参考色的最大通道差 ≤ 30 → 这个像素是背景，删掉
3) 只保留最大连通块（顺手丢掉右下角「豆包AI生成」那类水印）
4) 收边 1px，再**在 mask 上**羽化
```

> **知识点（这个项目最值钱的一条）：判据难写的时候，先回头想想能不能改输入。**
>
> 这个工具**前后写了四版**，前三版都在补算法：
>
> | 版本 | 做法 | 结果 |
> |---|---|---|
> | 一 | 按"颜色像不像背景"判定 | **整片失败**。脸的 `(253,240,225)` 和背景 `(253,246,230)` 只差 **5**，阈值定宽了切人物、定窄了留背景 |
> | 二 | 改**几何判据**：深色描边膨胀成"堤坝" → 从四边漫水 → 堤坝外面才是背景 | 人物能抠出来了。但边界切在描边外沿，**把角色自带的白色贴纸边一起切掉**；被轮廓围死的背景块（头发卷中间的洞）漫水进不去 |
> | 三 | 在二的基础上加"再抠一次"：用「到背景的距离 + 均色差 + 色散」三条判据去认那些被围死的块 | 能清掉大半，但**很脆**：实测同一块残留在 8 张图里的"颜色吻合度"能从 15% 跳到 79%（"离它最近的那块背景"会随人物姿势变），阈值怎么定都会漏或误伤。用户看完说"部分图像多扣"，整轮回退 |
> | **四** | **把素材背景换成纯青色**，算法退回"删掉背景色的像素" | **一句话解决。** 前三版的所有补丁全部删掉 |
>
> 前三版加起来几百行、踩了七八个坑（其中两个是把**整个角色**当残留删掉），
> 而真正的解法是把背景换成纯色。**算法难写，往往是输入没选对。**
>
> 顺带白拿的两件事：角色自带的白色描边**完整保留**（它是画稿的一部分）；
> 被围死的背景色也一视同仁地删掉 —— 逐像素判定，压根不问连通性。

> **知识点：给算法留一个能自动报警的指标，比每次都靠眼睛看强**
> 诊断里那条「边框内吻合 %」是"这张图适不适合这么抠"的客观指标 ——
> 背景是纯色时它是 100%，背景花的时候它会掉下来，程序这时会**明确警告**
> 而不是闷头给一张烂图。另一条「残留自检」同理：逐像素删的算法它必然是 0，
> 留着是为了万一哪天又改回"按块判定"，能立刻看出来。

> **知识点：参数默认值不能是绝对值（这条是踩出来的）**
> 已经被删掉的几何判据里有个"堤坝半径"，当时写死 5px —— 658px 宽的图够用，
> 1760×2304 的图就会漏：**浅色头发被整片当成背景吃掉，还不报任何错**。
> 修法是按图宽自动定。同一个坑在 `SheetGen` 的缩放上又踩过一次（见 3.2）。
> **查法**：把参数从小到大扫一遍，看诊断里最能反映问题的那个量 ——
> 漏的时候偏大，封住之后会出现一个明显拐点，再大也没有副作用。
> 现在的键控算法没有这类尺度参数（只有颜色容差，和图像大小无关），
> 但**换素材、换分辨率之后仍然要重新确认一遍**。

> **知识点：羽化要在 mask 上做，不要在彩色图上做**
> 如果对彩色图做羽化，半透明的边缘像素拿到的是"人物色和背景色混出来的浅边"，
> 于是需要额外做 despill（去背景溢色）。而把羽化作用在 mask 上，
> 边缘像素保留的是**人物自己的**颜色，白边从源头上就不存在。
> 再配合往里收 1px（`ErodePx`），抗锯齿的混色边也一起去掉了。

**两个必须知道的边界**（换素材后要专门确认）：

- **颜色容差不能乱放。** 实测人物里最接近背景色的是**眼睛的青蓝高光**，和青色差 **45**；
  放到 45 以上就会把眼睛高光当成背景挖掉。现在取 30，两边都有余量。
- **"只留最大连通块"会丢掉"不是背景色、又和人物不连通"的东西。**
  实测素材右下角的水印就是这么没的（9 张图里被丢掉的块**全部**在那个角上，
  没有一块落在人物身上）。要是画稿里有**离开人物的**装饰（飘带、星星、独立道具），
  这一步会把它丢掉，那时得把"只留最大"改成"按面积筛"。

**中间产物必须能肉眼检查**（不然这种算法没法调）：

| 文件 | 看什么 |
|---|---|
| `<名>.png.check.png` | 合成到**洋红底**上 —— 残留的背景色一眼可见（洋红是刻意的：青/奶油色都藏不住） |
| `<名>.png.mask.png` | 原图上把"判成背景抹掉"的像素涂红 —— 抠漏人物一眼可见 |
| `<名>.png.alpha.png` | 只看 alpha 的灰度图 |
| `<名>.png.txt` | 数值诊断：bbox、背景占比、**背景参考色 + 边框内吻合度**、被丢掉的连通块、**残留自检** |

**自动流程不可能 100% 干净**，所以留了修正文件（每行一条，可复现）：
`erase x y [容差]`（把某块抹掉）/ `keep x y`（把误抹的恢复）。

### 3.2 生成 sheet（SheetGen.cs）

- 布局：一行一个状态，8 列（= 帧数最多的状态），单帧 288×384 px（= 144×192 逻辑 ×2）。
- 动作：**同一个姿势的平移/旋转/缩放**，不是重画的帧。所以参数描述的是
  "这个状态要什么感觉"（idle 收敛、alert/done 才允许大跳），**与具体画稿无关** ——
  换姿势图不用改动作参数。
- 符号（「…」「!」★）用代码画，画在人物变换**之外**（不跟着跳）。
- 清单由**生成器自己写**。

> **知识点：缩放比例要每个状态各自算，不能全局共用一个像素比**
> 我一度为了"切换状态时大小不跳"改成全局共用 —— 等加入一张低分辨率图
> （658×860 和另外三张 1760×2304 混用）之后，那张就显示得**只有别人 1/3 大**。
> 素材分辨率不一致时，全局共用像素比一定是错的。按各自的图适配到最大才对。

> **知识点：把 bbox 高度统一 ≠ 看起来一样大**
> 站姿里"头"只占 bbox 高的 ~23%，盘腿/蜷缩的姿势能占 ~32%。于是同样 bbox 高度下，
> **坐着的那几个头明显更大**，看着像"她凑近了"。实测数据有意思：调之前
> sleep/error 的**头宽**其实和站姿一样（比值 0.996/1.016），所以这纯粹是
> "坐下来该画小一点"的审美惯例 —— 现在用 `ScaleAdjust()` 手工微调
> （think 0.93 / sleep 0.88 / error 0.86 / work 1.05），**改完必须看图验收**，
> 它不是算出来的。

> **知识点：清单必须由生成器写**
> 手改 `frameWidth` 却忘了改 sheet（或反过来）→ 切出来是一堆错位的碎图，而且**不报错**。
> 所以 `SheetGen.exe` 和 `MakeSprites.exe` 各自写自己的清单，谁生成谁负责。

### 3.3 调试工具（Zoom.exe）

分层窗口截不到图，而整张检查图看起来只有几百像素宽，边缘细节根本看不清。
`Zoom.exe 图 <x> <y> <w> <h> <倍数> <输出.png>` 裁一小块放大，
**给两张图就并排对比**（坐标共用，所以"原图 vs 抠图"天然对齐）。

---

## 4. 与 Claude Code 的对接

### 4.1 hook 配置

`install-hooks.ps1` 会先备份 `~\.claude\settings.json`，然后写入 8 个 hook 事件。
每个 hook 长这样：

```json
{ "type": "command", "command": "D:\\ClaudePet\\bin\\PetNotify.exe", "args": ["awake"], "timeout": 5 }
```

> **知识点：为什么必须用 exec 形式（`args` 分开写）**
> 如果把状态拼进 command 字符串（`"D:\\ClaudePet\\bin\\PetNotify.exe awake"`），
> Claude Code 会把它交给 shell 解析，而 Git Bash 里未加引号的反斜杠是转义符 ——
> 路径被吃成 `D:ClaudePetbinPetNotify.exe`，于是每个 hook 都报 `command not found`，
> 桌宠全程收不到事件。**`args` 存在时是 exec 形式**：直接 spawn，不经过任何 shell。

### 4.2 异步握手

`PetNotify.exe` 第一行先打印 `{"async": true, "asyncTimeout": 10000}`，
Claude Code 看到就不再等这个 hook 跑完 —— 否则每个工具调用都要等一个进程启动
（60~130ms）才能继续，会话会明显变慢。

### 4.3 事件 → 状态

| Claude Code 事件 | 状态 | 表现 |
|---|---|---|
| `SessionStart` | awake | 双手握拳小跳 + 气泡「会话开始 · 项目名」 |
| `UserPromptSubmit` | think | 盘腿托腮 + 「…」 |
| `PreToolUse` | work | 坐书桌写字 + 快节奏小抖 |
| `PostToolUse` | think | 回到思考 |
| `PostToolUseFailure` | error | 抱膝缩着发抖 |
| `Notification`(permission) | alert | 挥手 + 「!」（一直举着，直到你处理） |
| `Notification`(idle) | sleep | 抱枕头睡在云上 |
| `Stop` | done | 举拳欢呼 + ★ |
| `SessionEnd` | sleep | 同上 + 气泡「会话结束 · 项目名」 |

`cwd` 字段被用来显示项目名（`会话开始 · ClaudePet`），多个会话同时开时一眼能看出是哪个。

---

## 5. 编译环境（零安装）

本机没有任何开发工具链，用的是系统自带的 C# 编译器：

```
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe     ← 只支持 C# 5
运行时：.NET Framework 4.x（Windows 自带）
WPF 程序集：从 GAC 引用（注意 PresentationCore 在 GAC_64，不在 GAC_MSIL）
```

**C# 5 的限制**（写代码时会一直碰到）：不能用 `$"..."` 插值、`?.`、`nameof`、
表达式体成员；`async/await` 可以用。

四个会让报错信息变得很有误导性的坑（详见 README 的「已知坑」）：

1. `csc` 把 `/` 当选项前缀，**即使它出现在路径中间**（`D:/a/b.cs` 被拆成 `D:` + `b.cs`）→ 路径一律用反斜杠。
2. Git Bash 会把 `/nologo` 这类参数转换成路径 → `export MSYS_NO_PATHCONV=1`。
3. 别显式引用 `System.Web.Extensions`（`csc.rsp` 已经引了，再引报 CS1703「重复的程序集」）。
4. 含中文的 `.ps1` 必须存成 **UTF-8 with BOM**，否则 PowerShell 5.1 按 GBK 解析、直接语法报错。
   （而 `.cs` 源文件里的中文字符串**不需要** BOM，csc 读 UTF-8 是正确的 —— 两条规则相反。）

### 5.1 共用代码：共用源码，不是共用 DLL

6 个 exe 是各自独立编译、能单独拷走用的。但要共用代码时，做成 `Common.dll` 就会在
`bin\` 里多一个**必须跟着走**的文件 —— 破坏"每个工具都能单独拿走"这个优点。

csc 支持一次编译多个源文件，所以折中是 `src\Common.cs`：

```bash
SHARED="src\\Common.cs"
"$CSC" $COMMON -target:exe -out:"bin\\Cutout.exe" "src\\Cutout.cs" "$SHARED" "${WPF[@]}"
```

`Common.cs` 被**编进**每个 exe，外部依赖依然是零。代价是源码改一处要重编译所有 exe
（本来就要），以及**同一个类的多份副本**（调试时注意别拿错 exe 的堆栈）。

只有 `PetNotify` 不带它：`Common.cs` 用了 WPF 类型，而 `PetNotify` 跑在 hook 热路径上
刻意不引用 WPF（它也没有任何图片操作）。

合并之前，PNG 保存有 5 份、建目录有 4 份 —— 其中 `Cutout.WritePngSize` 和
`Zoom.WritePng` 是**逐字相同**的两份。这种重复的危险不在"难看"，而在
**改一边忘一边**，而症状是"某个工具的输出突然和别人不一样"。

---

## 6. 验证方法（这个项目里"怎么知道它是对的"）

因为窗口截不到图，"改动有没有生效"本身就成了一个课题。用过的办法：

| 目的 | 办法 |
|---|---|
| 看界面渲染 | `Pet.exe --shot <状态> <png>` 离屏渲染（分层窗口唯一的可视化验证） |
| 读界面上的**文字** | UI Automation：`AutomationElement.RootElement` + `ProcessIdProperty` 找窗口，`FindAll(Descendants)` 取 `Current.Name`（气泡内容就是这么读的） |
| 看窗口的**位置/尺寸/可见性** | `EnumWindows` + `GetWindowRect`（注意 `Process.MainWindowHandle` 对 `ShowInTaskbar=false` 的窗口返回 **0**，必须自己枚举） |
| 看抠图边缘 | `Zoom.exe` 裁块放大对比 |
| 模拟鼠标 | 必须用 `mouse_event`/`SendInput` 真注入；`PostMessage` 合成的窗口消息 **WPF 不认**（它按真实鼠标状态判断） |
| 抓 UDP 载荷 | 临时杀掉桌宠，自己 `UdpClient(47821)` 收一个包看发的是什么 |
| 验**右键菜单** | `tools\verify-menu.ps1`：真鼠标注入右键打开菜单，再用 UI Automation 把菜单项读出来核对。**子菜单要用 `ExpandCollapsePattern` 显式展开** —— 折叠时里面的项没被实例化，UIA 读不到，靠鼠标悬停会时通时不通 |
| 验**全屏自动隐藏** | `tools\verify-fullscreen.ps1`：自己造一个**精确铺满显示器**的假全屏窗口，不用真开游戏 |
| 验**点它不抢焦点** | `tools\verify-nofocus.ps1`：真鼠标注入点一下，比对点击前后的**前台窗口**（不是看返回值） |
| 验**重构没改行为** | 从 git 取上一版源码编出旧 exe，新旧同输入**比输出字节**。做过一次 13 项全一致（Cutout 3 图×5 输出 / Zoom / SheetGen / MakeSprites / `Pet --shot` 3 状态）。比"编译过了"强得多 |
| 认准**哪个窗口是桌宠** | 必须按**尺寸**筛（物理像素下约 200 宽）。不能"取第一个 `HwndWrapper`"：菜单的点击接收层是整屏大的、且只 Hide 不销毁，打开过一次菜单它就一直在 —— 会让全屏测试报假 FAIL，让"不抢焦点"测试**假装通过** |
| 问窗口的"立场" | `SendMessage(WM_MOUSEACTIVATE)` 看返回值 —— ⚠️ **这条路后来被证明是错的**：返回值没错，焦点照样被抢（见 2.1 / README 坑 18 教训②）。改成上面那条"点完看前台窗口变没变" |
| **编译到底成没成功** | **看 `build.sh` 的退出码**（`./build.sh; echo $?`）。别 grep 它的输出：中文是 GBK，Git Bash 里 grep 会把它当二进制文件、只回一句 `Binary file matches`，**错误行全被吞掉** —— 我就这么拿旧 exe 测了半天 |

> **两个反直觉的验证陷阱**
> ① 用注入鼠标测**拖拽**是测不准的：注入的"松开"收尾不干净，窗口会继续跟着真实光标跑，
> 于是 config.json 里会冒出离谱坐标（实测出现过 `1102,156`）。**拖拽只能人工验。**
> ② 我每调一次工具，自己的 hook 就会把气泡覆盖成「正在跑命令」——所以验证"单击后气泡显示什么"
> 必须在**同一个进程里点完立刻读**，否则读到的是我自己的活动。（反过来说，这也证明 hook 链路是通的。）

---

## 7. 已知限制 / 还没做的

- **会话中途启动桌宠时，它不知道当前状态**（要等下一个 hook 事件）。Claude Code 没有查询接口，
  要解决只能去读 transcript 文件，太脆，没做。
- `error` / `sleep` 的装饰（×、Z z、爱心）在抠图时被当独立元素丢掉了 —— 姿势本身表达得够清楚，
  但如果想要那些符号，可以像「…」「!」★ 那样用代码画。
- 多显示器位置处理较粗（只按虚拟桌面矩形判断，没处理显示器之间的"死区"）。
- **窗口整个矩形都在"吃"鼠标事件**（160×220，比人物本身宽 16px、下沿还有 28px 气泡带）。
  点在人物旁边的空白上不会穿透到桌面，整块也都能用来拖动 —— 这是有意的取舍（好抓、好拖），
  但如果你希望"只有人物本身响应"，那就要做点击穿透模式（下面那条）。
- 点击穿透模式（平时不挡鼠标点击、热键临时恢复）没做 —— 需要 P/Invoke 改窗口样式 + 全局热键，
  热键注册失败会得到一只"点不动的宠物"，要做就得做稳。
- 缩放上限 3.0（右键菜单里 50%~200%，配置文件里可以写更大，但会被 `ApplyScale` 夹住）。

---

## 附录：一次代码审查的记录

2026-09-12，请了一位**没参与写代码**的审查员通读全部源码，报回 16 条；逐条复核后修了 12 条，
3 条有意保留（写进「已知限制」），1 条是虚惊。

**最值钱的一条**：项目最核心的承诺（"点它不抢焦点"）其实从来没生效 —— 见 2.1 那个坑。
教训：**文档里的断言也要验证，尤其是"最重要的那条"**。它是从哪继承来的，不代表它成立。

**其余问题归成四类**（这种分类比具体条目更有用）：

| 类别 | 例子 | 怎么防 |
|---|---|---|
| **静默失败** | 端口写成非法值时监听线程抛异常被吞掉 → 看着一切正常、其实一个事件都收不到；修正文件路径写错时静默跳过，"抠图还是老样子"背上算法没调好的锅 | 每写一个 `catch {}` 都问一句"用户能看见这个失败吗"；兜底可以吞异常，但**要留个出口**（提示卡片 / 气泡 / 诊断文件） |
| **半途失败留下坏状态** | 素材加载到一半抛异常 → `anims` 空了但 `state` 还是 idle → 下一个事件再抛一次 → 进程静默消失 | 加载类操作**先读进局部变量、全部成功后再整体换上**（事务式）；对外的入口函数不信任任何输入（事件名、配置、清单都算输入） |
| **写死的名单与现实不一致** | 状态白名单挡住了清单里真有的新状态（还让 README 的"加状态不用改代码"成了假话）；`Environment.TickCount` 假定 32 位不会回绕 | 名单/顺序尽量从数据来；非写死不可的地方，在旁边注明"什么情况下会失效" |
| **自己验证不到的地方** | 分层窗口截不到图（只有离屏渲染能看到界面）；注入鼠标测不准拖拽 | 提前设计验证手段（第 6 节）；**分工验证**：自己写的代码请别人通读，盲区不一样 |

**一条经验**：审查员报的几条我一开始觉得"不可能"，实测之后**全是真的** ——
我自己早先甚至打印过那个窗口样式（`exStyle=0x00080008`，没有 NOACTIVATE）却没看出来。
**数据和自己预期不符时，停下来多看一眼那行数据**，比事后从现象反推便宜得多。

---

## 8. 如果照着做一个（可复用的配方）

1. **先定"不打断用户"这条红线**：不抢焦点、提示不用模态框、失败要说话（不能静默）。
2. **状态机 + 数据化素材**：状态名 → 动画行的映射写进清单文件，程序只认清单。
   状态缺失一律回退到安全默认值，**永远不崩**。
3. **一次性动画要能自动回到静止**：`pendingState` + 衰减表 + 一个"卡太久就回 idle"的兜底。
4. **素材流水线要留能肉眼检查的中间产物**，并且把"自动跑不干净"当成常态，
   给一个可复现的手工修正入口。
5. **抠图这种"判定"问题，先想清楚失败模式**：颜色判据在浅色背景+浅色角色上必失败；
   几何判据（用描边当堤坝）才是稳的。参数要随输入规模缩放，且用扫描找拐点，别靠推理。
6. **验证手段要提前设计**：不能截图的窗口，就用离屏渲染 + UI Automation。
7. **把踩过的坑写进 README**（尤其是报错信息有误导性的那些）——
   这个项目里 README 的「已知坑」比代码本身更能省时间。
