# 换行符。这个文件只有一条规则，但它防的是一个**真会翻车**的问题。
#
# 本机全局 core.autocrlf = true，含义是"检出时 LF→CRLF"。而 build.sh 是给
# Git Bash 跑的 —— 一旦被换成 CRLF，bash 会报：
#     ./build.sh: /bin/bash^M: bad interpreter
# 或者 $'\r': command not found，编译脚本直接废掉。
# 这个问题不会在提交时出现，而是在**某次 clone / git checkout / git reset --hard
# 之后突然出现**，那时候你早忘了有这回事。
#
# 只护 .sh 就够了：.cs / .ps1 / .md 用 CRLF 也能正常工作，
# 给它们也加规则只会平白让 git 多干活。
*.sh text eol=lf

# 2026-09-13 从零 clone 验收时加的一条：**pet.json 也要 eol=lf**。
#
# 起因是一个"验收噪音"：clone 出来跑一遍 SheetGen，`git status` 就报
# `M assets/pet.json` —— 而内容其实**一字未变**（和 `git show HEAD:` 比 md5 完全相同，
# 而且 `git add` 一下就恢复干净）。
# 根因是 autocrlf 的两头对不上：检出时 git 把 pet.json 写成 CRLF，
# 而 SheetGen 写的是 LF，于是 git 认为工作区"不是它该有的样子"。
#
# 为什么要修而不是忍：这个项目的**收尾验收流程本身就是**
# 「clone → 编译 → 重新生成 → 和原项目比字节」，而这一步正好会撞上它 ——
# 报出一个看起来像"SheetGen 不确定"的假警报，把人往"工具不可复现"这个错方向带。
# **验收流程里的假警报比不报还糟**，所以把工作区的行尾钉死成生成器实际写的那个。
*.json text eol=lf
